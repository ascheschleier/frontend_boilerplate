{{> meta/head }}

{{> partials/nav }}

{{> partials/header }}
{{> partials/hero }}

<section class="content">
    <div class="container">
        {{#item}}
            {{> partials/entry-small }}
        {{/item}}
    </div>
</section>

{{> partials/footer }}

{{> meta/footer }}